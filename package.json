{
  "name": "skyglide",
  "version": "0.0.1",
  "description": "Cozy bird‑flying game served by a single Cloudflare Worker",
  "type": "module",

  "scripts": {
    "deploy": "wrangler deploy --minify"
  },

  "dependencies": {},

  "devDependencies": {
    "wrangler": "4.7.0"
  }
}
